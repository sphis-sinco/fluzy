# Fluzy

TBA

# Source Code

## Setup

1. Clone the github repo
2. Open the github repo in your code editor
3. Run `git submodule update --init --recursive`
4. Install the libraries via `dev/libraries.bat`
5. You should be good then
